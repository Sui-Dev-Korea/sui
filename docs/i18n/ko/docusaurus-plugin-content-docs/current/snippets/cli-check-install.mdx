## Check Sui CLI installation

Sui CLI를 사용하기 전에 먼저 설치가 진행되어야 한다. 시스템에 CLI가 설치되어 있는지 확인하려면 터미널이나 콘솔을 열고 다음 명령어를 입력한다.

```sh
$ sui --version
```

터미널이나 콘솔에 버전 번호가 표시되면 Sui CLI가 이미 설치된 것이다.

명령어를 찾을 수 없는 경우, [Install Sui](/guides/developer/getting-started/sui-install.mdx) 안내에 따라 시스템에 Sui CLI를 설치한다.
