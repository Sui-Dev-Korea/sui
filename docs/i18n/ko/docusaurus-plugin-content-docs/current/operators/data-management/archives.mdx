---
title: Sui 아카이브
sidebar_label: Sui 아카이브
description: 아카이브는 Sui의 모든 트랜잭션 기록이다. 풀 노드에서 아카이브를 활성화하는 것이 모범 사례이다.
keywords:
  - archive
  - archive data
  - full node archive
  - node archive
  - fallback
  - archival fallback
---


Sui 아카이브는 Sui의 모든 트랜잭션 데이터 이력이다. 경우에 따라 피어 노드가 최신 몇 개의 에포크보다 더 뒤처진 경우, 동기화를 통해 모든 트랜잭션과 effects를 따라잡지 못할 수 있다. 이러한 경우, 피어 노드는 동기화에 의존하는 대신 아카이브에서 관련 정보를 다운로드하는 방식으로 폴백할 수 있다.

:::info
1.51 릴리스부터 mainnet의 기본 아카이브 버킷은 요청자 부담 모델로 전환된다. 구성 예시는 아래를 참고한다.
:::

`https://checkpoints.mainnet.sui.io`는 최근 30일 checkpoint만 보존하며, 전체 이력 Mainnet backfill에는 적합하지 않다. 새 fullnode를 세울 때는 항상 snapshot에서 시작한다.

## archival fallback 설정 {#set-up-archival-fallback}

노드가 지연될 때 아카이브로 폴백할 수 있도록 하려면, `fullnode.yaml` 파일에 다음을 추가한다:

<Tabs groupId="object-store">
<TabItem value="mainnet" label="Mainnet">

```yaml
state-archive-read-config:
  - ingestion-url: "https://s3.us-west-2.amazonaws.com/mysten-mainnet-checkpoints"
    # How many objects to read ahead when catching up
    concurrency: 5
    remote-store-options:
      - ["aws_access_key_id", <YOUR_AWS_ACCESS_KEY_ID|PATH_TO_AWS_ACCESS_KEY>]
      - ["aws_secret_access_key", <AWS_SECRET_ACCESS_KEY|PATH_TO_AWS_SECRET_ACCESS_KEY>]
```

</TabItem>
<TabItem value="testnet" label="Testnet">

```yaml
state-archive-read-config:
  - ingestion-url: "https://checkpoints.testnet.sui.io"
    # How many objects to read ahead when catching up
    concurrency: 5
```

</TabItem>
</Tabs>
